{
  "rules": {
    ".read": true,
    ".write": "auth != null",
    "invites": {
      "$inviteId": {
        "guestId": {
          ".write": "auth != null && !data.exists()",
          ".validate": "newData.isString() && !data.exists()"
        }
      }
    }
  }
}
